import type { ReactNode } from "react";
import "./footer.css";

export function Footer(): ReactNode {

    return (
        <footer className="reveal">
            <div>
                
            </div>
        </footer>
    );
}